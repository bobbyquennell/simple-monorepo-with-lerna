{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint --parallel",
    "lint:fix": "lerna run lint:fix --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.3.1",
    "typescript": "^4.3.2"
  }
}
